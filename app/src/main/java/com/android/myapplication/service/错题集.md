
1、**context**.bindService(mAudioIntent,mAudioServiceConnection,Context.BIND_AUTO_CREATE)
    **和context**.unbindService(mAudioServiceConnection);
####     的context需得是同一个，不然会解绑失败，提示Exception“Service not registered”